package org.data2semantics.platform.core;

public class MessagePackingUtil {

	
	// Serialize using MessagePacker the current instance input modules, now do we create one serialize inputs for all inputs
	// or one file for each inputs ? What if the inputs are large ?
	public static void serializeInputs(ModuleInstance instance, String intermediateInputFileName){
		
	}
}
